- hosts: all
  become: yes
  vars:
    ferm_combine_rules_excludes:
      - ferm_rules_group_child_group
  tasks:

    - include_role:
        name: ansible-role-ferm

    - assert:
        that: ferm_rules == config
      vars:
        config:
          01-one:
            - content: |
                # parent group
          02-two:
            - content: |
                # parent group
          03-three:
            - content: |
                # host
