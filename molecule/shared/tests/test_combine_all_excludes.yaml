- hosts: all
  become: yes
  vars:
    ferm_combine_rules_excludes:
      - ferm_rules_group_child_group
  tasks:

    - include_role:
        name: ansible-role-ferm

    - assert:
        that: ferm_rules == config
      vars:
        config:
          01-one:
            '00':
              content: |
                # parent group
          02-two:
            '00':
              content: |
                # parent group
            '01':
              content: |
                # parent group
          03-three:
            '00':
              content: |
                # host
            '01':
              content: |
                # host
